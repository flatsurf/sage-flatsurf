from distutils.core import setup

setup(name='Flat surfaces',
       version='0.1',
       description='flat surfaces',
       packages = ['flatsurf', 'flatsurf.geometry', 'flatsurf.graphical']
       )
